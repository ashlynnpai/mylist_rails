.col-sm-6
  %h4#heading Not Watched
  .unwatched_list
    - @unwatched_casts.each do |railscast|
      .col-sm-1
        = railscast.cast.episode
      .col-sm-7
        = link_to railscast.cast.title, railscast_path(railscast)
      .col-sm-2
        = link_to image_tag('notwatched.png'), toggle_watched_path(id: railscast.id, cast_id: railscast.cast.id, user_id: current_user.id, watched: true), method: :put
      .col-sm-2
        - if railscast.favorite?
          = link_to image_tag('fav.png')
        - else
          = link_to image_tag('unfav.png')
      %br
 
.col-sm-6
  %h4#heading Watched
  .watched_list
    - @watched_casts.each do |railscast|
      .col-sm-1
        = railscast.cast.episode
      .col-sm-7
        = link_to railscast.cast.title, railscast_path(railscast)
      .col-sm-2
        = link_to image_tag('notwatched.png'), toggle_watched_path(id: railscast.id, cast_id: railscast.cast.id, user_id: current_user.id, watched: false), method: :put
      .col-sm-2
        - if railscast.favorite?
          = link_to image_tag('fav.png')
        - else
          = link_to image_tag('unfav.png')

      %br


    
    